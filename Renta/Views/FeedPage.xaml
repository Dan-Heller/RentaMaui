<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Renta.FeedPage"
             BackgroundColor="White"
             Title="Renta"
            >
    
   <!-- <Shell.TitleView>
        <Image Source="homeicon.png" HorizontalOptions="Center"
               VerticalOptions="Center"/>
    </Shell.TitleView> -->
    
    
    
  <ContentPage.ToolbarItems>
        
        <ToolbarItem  IconImageSource="profileicon" Command="{Binding ProfileCommand}"/>
        <ToolbarItem   IconImageSource="coinicon" Command="{Binding CreditsCommand}"/>
    </ContentPage.ToolbarItems> 
    
    
    <StackLayout>
        <Label Text="Welcome to .NET MAUI!"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
    </StackLayout>
</ContentPage>